## 1. Spring Security

By default, Spring Security will protect every API and MVC path available inside our project.
It will generate a default username and randomly generated password, which can be configured in the configuration file (application.properties or application.yaml).
